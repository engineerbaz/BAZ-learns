# RedHat OpenShift

 
Go to [redhat.com/openshift/create/local](url) to download OpenShift Local

<br>
Allocate extra memory to VM by ```crc config set memory 14336```

enable monitoring vy ```crc config set enable-cluster-monitoring true```
